[build]
  base = "app"           # se sua pasta principal for "app"
  command = "npm run build"
  publish = "app/dist"   # ou "app/build" se usar React padrão

[build.environment]
  NODE_VERSION = "18"
